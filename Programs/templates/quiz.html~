<!DOCTYPE html>
<html>
<head>
    <title>Quiz</title>
</head>
<body>
    <h1>Quiz Question</h1>
    <p>{{ question }}</p>
    <form method="post">
        {% for choice in choices %}
        <button type="submit" name="choice" value="{{ loop.index0 }}">{{ choice }}</button><br>
        {% endfor %}
    </form>
</body>
</html>
